version: '3.8'

services:
  server:
    image: veredins-server:latest
    container_name: veredins-server   # ← Nome padrão fixo aqui!
    ports:
      - "12345:12345/udp"
    restart: unless-stopped           # opcional: reinicia se cair
    # environment:                    # se precisar de vars como PORT
    #   - PORT=12345